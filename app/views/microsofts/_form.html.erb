<%= form_with(model: microsoft, local: true) do |form| %>
  <% if microsoft.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(microsoft.errors.count, "error") %> prohibited this microsoft from being saved:</h2>

      <ul>
        <% microsoft.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
